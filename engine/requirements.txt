# NSE Alpha Engine — Python dependencies
# Install with: pip3 install -r requirements.txt

# ── REST API server ───────────────────────────────────────────────────────────
fastapi==0.115.12          # High-performance async REST framework
uvicorn[standard]==0.34.0  # ASGI server with uvloop + httptools
pydantic==2.11.1           # Data validation (used by FastAPI models)

# ── Data acquisition ──────────────────────────────────────────────────────────
yfinance==0.2.54           # Yahoo Finance OHLCV downloader (.NS tickers)
pandas==2.2.3              # DataFrame IO (yfinance dependency)
requests==2.32.3           # HTTP client for Stooq + Alpha Vantage

# ── pybind11 C++ bindings (build-time only, already compiled) ─────────────────
# pybind11 is a header-only library used at compile time via CMakeLists.txt.
# The compiled .so module ships in engine/build_output/ and is imported
# directly — no runtime pybind11 pip package is required.
